name: FleetFlow_test

on:
  push:
    branches: flteeflow5-security
  pull_request:
    branches:  flteeflow5-security

jobs:
  pipeline:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8
        env:
          MYSQL_ROOT_PASSWORD: 2005085
          MYSQL_DATABASE: fleetflow
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h localhost -p2005085"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Wait for MySQL
        run: |
          echo "Waiting for MySQL..."
          for i in {1..10}; do
            nc -z localhost 3306 && echo "MySQL is up!" && exit 0
            sleep 5
          done
          echo "MySQL did not start" && exit 1

      - name: Compile and Test
        env:
          SPRING_DATASOURCE_URL: jdbc:mysql://localhost:3306/fleetflow
          SPRING_DATASOURCE_USERNAME: root
          SPRING_DATASOURCE_PASSWORD: 2005085
        run: mvn clean verify

      - name: Save JAR
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: fleetflow-executable
          path: target/*.jar